{
  "name": "saas-website-manager",
  "version": "1.0.0",
"scripts": {
  "build": "mkdir -p dist && tailwindcss -i ./src/input.css -o ./dist/output.css --minify && cp ./src/index.html ./dist/index.html"
}
,
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
