{
  "name": "saas-website-manager",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && tailwindcss -i ./src/input.css -o ./dist/output.css --minify && cp index.html dist/ && cp -r img dist/ && cp -r js dist/"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
